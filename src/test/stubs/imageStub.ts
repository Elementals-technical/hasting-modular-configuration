/**
 * Stub for static image imports (png/jpg/svg/...) under the node test environment.
 * Mapped in `vitest.config.ts` so modules that import images (e.g. the preset
 * catalog) can be loaded in tests without a real asset transform.
 */
export default "";
